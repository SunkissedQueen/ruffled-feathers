import React from 'react';
// because it lives in its own folder need the ES6 function vice export default
export const Header = () => {
  return (
    <h1>Header Page: A Basic Functional Component</h1>
  )
}